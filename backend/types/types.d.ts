declare module 'package-to-zip' {
    // Add any type definitions you know here, or leave it as `any` for now
    const content: any;
    export = content;
  }

  declare module 'tar-to-zip' {
    // Add any type definitions you know here, or leave it as `any` for now
    const content: any;
    export = content;
  }

